# 密碼保護系統使用說明

## 預設密碼

系統已設定兩組預設密碼：

1. **老師密碼**：`teacher123`
   - 用於保護：每日點名、月曆記錄、出勤總覽、請款系統

2. **HR 密碼**：`hr2024`
   - 用於保護：HR 報告頁面

## 如何修改密碼

如果您想修改密碼，請按照以下步驟：

### 步驟 1：生成新的密碼 Hash

1. 打開瀏覽器（Chrome、Edge、Firefox 等）
2. 按 `F12` 打開開發者工具
3. 切換到 "Console"（控制台）標籤
4. 複製並貼上以下代碼，將 `"您的新密碼"` 替換為您想要的密碼：

```javascript
async function generateHash(password) {
  const encoder = new TextEncoder();
  const data = encoder.encode(password);
  const hashBuffer = await crypto.subtle.digest('SHA-256', data);
  const hashArray = Array.from(new Uint8Array(hashBuffer));
  return hashArray.map(b => b.toString(16).padStart(2, '0')).join('');
}

generateHash("您的新密碼").then(hash => console.log("Hash 值:", hash));
```

5. 按 Enter 執行，會顯示一個長字串（這就是 hash 值）

### 步驟 2：更新密碼 Hash

1. 打開 `js/auth.js` 檔案
2. 找到 `PASSWORD_HASHES` 這個變數
3. 將對應的 hash 值替換為步驟 1 生成的新 hash 值
4. 儲存檔案

### 範例

假設您想將老師密碼改為 `mypassword123`：

1. 在瀏覽器控制台執行：
```javascript
generateHash("mypassword123").then(hash => console.log("Hash 值:", hash));
```

2. 假設得到的 hash 是 `abc123def456...`（實際會更長）

3. 在 `js/auth.js` 中修改：
```javascript
const PASSWORD_HASHES = {
  teacher: 'abc123def456...', // 替換為新的 hash
  hr: '8d969eef6ecad3c29a3a629280e686cf0c3f5d5a86aff3ca12020c923adc6c92'
};
```

## 使用方式

1. 第一次訪問任何受保護的頁面時，會彈出密碼輸入框
2. 輸入正確的密碼後即可訪問
3. 同一個瀏覽器視窗內，8 小時內不需要重複輸入密碼
4. 關閉瀏覽器後，下次訪問需要重新輸入密碼

## 注意事項

- 密碼經過 SHA-256 加密，即使查看程式碼也無法直接看到原始密碼
- 這是前端保護機制，主要防止一般使用者誤入
- 請妥善保管密碼，不要分享給未授權的人員

